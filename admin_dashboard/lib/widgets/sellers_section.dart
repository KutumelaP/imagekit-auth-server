import 'package:flutter/material.dart';

class SellersSection extends StatelessWidget {
  const SellersSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sellers Management Coming Soon!', style: TextStyle(fontSize: 24)),
    );
  }
} 