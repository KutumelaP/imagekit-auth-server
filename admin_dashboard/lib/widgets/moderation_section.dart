import 'package:flutter/material.dart';

class ModerationSection extends StatelessWidget {
  const ModerationSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Moderation Tools Coming Soon!', style: TextStyle(fontSize: 24)),
    );
  }
} 