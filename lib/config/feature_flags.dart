class FeatureFlags {
  // Toggle to enable the new Checkout V2 flow
  static const bool enableCheckoutV2 = false;

  // Progressive enhancements can be toggled independently as needed
  static const bool enableSellerManagedDelivery = true;
  static const bool enableUrbanOptimization = true;
  static const bool enableRuralOptions = true;
}

